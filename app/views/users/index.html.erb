<h1>Profiles</h1>
<p>
  <%= link_to "Add a new profile", new_user_path %>
</p>
<ul>
  <% if @users.nil? == false %>
  <% @users.each do |user| %>
  <li><%= link_to user.name, user_path(user) %></li>
  <% end %>
  <% end %>
</ul>
